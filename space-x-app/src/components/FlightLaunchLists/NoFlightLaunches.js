import React from "react";
import styles from "./NoFlightLaunches.module.css";

const NoFlightLaunches = (props) => {
  return <div className={styles["no-results"]}>No results to display.</div>;
};

export default NoFlightLaunches;
